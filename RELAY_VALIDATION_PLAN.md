# Relay：Challenge 的应对与验证方案

状态：产品建议和试点设计，尚未实施或获得客户证明。2026-09-05。

## 1. GTM：先验证一个客户的重复任务

建议先服务一个需要补充外部数据的 underwriting 产品团队。使用者是业务或产品人员，接入者是工程团队，预算批准者需要访谈确认。个人 underwriting 仍是首个场景，公司记录只适用于需要审查关联公司的申请人。

不预设大型 marketplace。先由团队手工招募少量设计伙伴，以一个明确数据缺口作为试点入口。首轮访谈建议覆盖 5–8 个目标团队，这是招募目标，不是市场规模证据。要求对方展示近期真实或脱敏的请求：缺什么数据、现有供应商为何不够、每月发生多少次、谁处理、花多少时间、由谁付钱。

有痛点的证据是现有流程的重复损耗；付费证据是愿意为有界的交付付费；留存证据是首批交付后再次使用。口头觉得有趣、免费注册和 Testnet 请求量均不算付费验证。

比较对象必须包括客户现有固定 API、聚合平台和人工处理。若固定供应商已经覆盖需求且几乎不需切换，缩小动态 supplier selection 的主张，验证故障备选或额外覆盖是否有价值。若均无价值，停止推进该场景。

获客先测试现有引荐和创始人直接销售；目前尚未确定具体可用渠道，等待团队提供客户资源。任何对外联系须由团队另行确认，本文未发送消息。

## 2. 供应：精选目录 + 适配器

先邀请 2–3 个允许代理采购的供应商，建立覆盖一种资源的目录。每条 offer 必须标明类别、国家覆盖、主体标识、字段、数据截至时间、用途限制、报价、最低消费、币种、网络、退款条件和支持方式。

先确认代理使用和转交数据的权限。连接器分别处理传统 API 和 x402 endpoint；统一的是请求与交付契约，不强制每个供应商上链。现有 demo 两个资源属于不同类别，不能据此声称同一请求已有两个可替代报价。

排序顺序：先满足用途、覆盖、时效、预算等硬条件，再按预计总成本排序。只有一个合格源时明确说明，没有合格源就拒绝购买。显示选择理由和 Relay 服务费，避免把平台利润率作为隐藏排序因素。

进入条件：至少有可交付且获准使用的资源。要验证“比价”，同一具体请求必须有至少两个合格可替代 offer。否则只验证自动采购。

## 3. 质量：区分契约检查、主体匹配和真实性证据

供应商负责其声明、原始数据来源和交付承诺；Relay 负责契约校验、主体匹配、证据记录和采购执行；客户定义适用场景及最终验收要求。高风险或无法证实的结果交由指定业务人员审核。

- 购买前：校验请求中的主体标识、国家、资源类别、需要的字段、数据时点、用途和预算。候选描述与需求逐项比对，返回满足 / 不满足 / 未知及理由，不输出无依据的综合置信度。
- 交付后：检查 JSON schema、字段完整性、单位、数据截至时间，以及准确的主体 ID。端点今天可用不等于数据今天更新。
- 内容证据：可以对 LEI 字段回查 GLEIF 官方 API；只证明与该源一致。若供应商原本也来自 GLEIF，这属于来源核对，不是独立交叉验证。真正独立验证要追踪第二个来源的上游，避免两个供应商复制同一份数据。
- 行业统计必须检查口径、地理范围、统计期间和单位；CPI 不能替代法律行业收入。缺少合适源就不交付为“匹配成功”。

建议返回：subject_match、contract_check、as_of、source、checked_by、checked_at、evidence、limitations、review_required。记录校验规则版本和证据。现在的固定 trust_score=80、freshness_days=0 不能作为这些检查的实现证明。

来源：[GLEIF API](https://www.gleif.org/en/lei-data/gleif-api) 基于 Golden Copy 数据。该来源不证明个人所有权关系或个人收入。

## 4. Agent 与失败处理：有限授权 + 可恢复状态

采购前固定 request_id、幂等键、需求指纹、报价有效期、最大总价、允许供应商和允许结算方式。服务端政策限制 Agent 权限；价格变化或没有匹配资源就停止。

建议状态：quoted、authorized、payment_pending、paid、delivered、accepted、disputed、refunded。客户额度先预留，交付验收后结算。付款后超时必须先核对已有交易，不直接重新付款。允许获取同一已支付资源的交付重试。

注入故障测试：重复请求、超时、价格过期、错误主体、陈旧数据、供应商无响应、已付未交付。每个测试都要能找到订单、交易和责任状态。没有供应商退款安排时，Relay 要为客户补偿建立准备金；链上交易不会因为 API 失败自动撤销。

先 shadow mode（只推荐、不买），通过人工对照后再进行限额采购。一次样本测试不能证明 production reliability。

## 5. 支付：USD 客户体验与供应商结算分离

建议首个付费试点采用 USD 用量套餐预付，或对获批客户汇总账单；Relay 服务端维护额度预留与扣减账本，避免每个微小查询都发起一笔独立银行卡收款。充值额度和账期由实际费用、客户使用量和退款安排决定，不先定 US$10。

Stripe 的 billing credits 提供预付用量计费参考，但并非现成的供应商付款钱包，也不能据此声称完全满足 Relay 的实时授权需求。采用前仍需核对产品适用性、费用及运营条件。

供应商接受什么就使用什么：支持的 XRPL 路径执行 XRP 或明确支持的资产；传统 API 使用已约定的付款安排。不得把“接受 USDT”等同于“接受 XRPL 上的任意同名资产”。核对资产标识、网络、收款地址和实际 facilitator 路径。

验证链路价值：在相同数据任务、相同批量条件下，对比 XRPL 路径、API 预付和月结。测到账及可用数据时间、完整费用、失败率、资金占用和对账工作。快速结算只是技术属性，最终收益仍需实测。

来源：[Stripe billing credits](https://docs.stripe.com/billing/subscriptions/usage-based/billing-credits)。[x402 网络支持](https://docs.x402.org/core-concepts/network-and-token-support) 明确区分协议、运行时和生产结算支持；协议支持并不保证当前部署可用。

## 6. 完整成本：先记账，再定价

使用实际成功交付数作分母，并把失败尝试的成本计入同一批任务，避免只展示成功单的费用。

客户基线 = 数据费用 + 实际处理时间 × 约定人力成本 + 集成维护分摊 + 支付对账 + 失败返工。
Relay 路径客户成本 = 客户向 Relay 支付的费用 + 客户剩余处理时间成本 + Relay 接入维护分摊。

Relay 批次贡献 = 批次净收入 − 所有供应商付款 − 收款 / 兑换 / 网络费用 − 校验 / 计算 − 退款净损失 − 可归属支持成本。退款净损失须扣除已从供应商追回部分，避免重复计算。
月经营结果 = 月贡献 − 固定工程、销售、基础设施和运营成本。已归入固定工资的工时不得再次计为现金可变成本；同时可单列容量分析。

获客成本单列：获取付费客户的销售现金支出及分摊投入 / 新增付费客户数。早期样本不足时不推算稳定 LTV。

比较低 / 基准 / 高用量，以及更多人工检查、兑换成本上升、供应商涨价和退款增加的情景。未测参数保留为空或作为明确的情景输入，不能填入看似真实的市场数字。

定价先测试“供应商费用 + 明示采购服务费”，比隐藏大额差价更容易解释。服务费水平仍需客户付费试验。没有正贡献时不计算有意义的盈亏平衡销量；有正贡献时，盈亏平衡成功交付量 = 月固定成本 / 平均单笔贡献。

## 7. 建议的分阶段试点

1. 访谈和基线：确认一个付费决策人、重复任务、现有成本及允许使用的数据。没有重复痛点则停止该场景。
2. 供应准备：确认可用源、用途权限和验收契约。两个可替代源齐备后才测试比价。
3. Shadow 对照：用脱敏或合成请求验证匹配。客户指定人员给出验收答案，记录未知和不一致。无法建立参考答案的字段不声称已验证。
4. 限额付费试点：双方事先确定预算、成功条件、退款边界和观察周期。观察相同任务的成功率、总成本和复购。
5. 决策：客户有节省且 Relay 有正贡献，才扩量；获客与固定成本回收仍需更多时间。若仅支付路线不合适，保留采购功能并调整结算路径。

需要团队补充：最可接近的客户群及引荐资源。工资、预算、期望收费和付款条件在执行试点前补齐，不影响现阶段方案设计。
